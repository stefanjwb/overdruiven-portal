# Core
fastapi==0.141.1
starlette==1.6.0  # pinned explicitly: fastapi has no upper bound, and 0.x has unpatched CVEs (fixed only in 1.0.1+/1.1.0/1.3.0/1.3.1)
uvicorn[standard]==0.52.4
sqlmodel==0.0.42
pydantic-settings==2.15.0

# Auth
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
python-multipart==0.0.32
itsdangerous==2.2.0

# Email validation
email-validator==2.3.0

# HTML sanitization (blogposts)
nh3==0.3.7

# Google Calendar
google-api-python-client==2.200.0
google-auth==2.58.0

# Rate limiting
slowapi==0.1.10

# Environment
python-dotenv==1.2.3
